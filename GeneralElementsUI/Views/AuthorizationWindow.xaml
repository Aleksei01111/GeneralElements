<Window x:Class="GeneralElementsUI.Views.AuthorizationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:GeneralElementsUI.Views"
        d:DataContext="{d:DesignInstance Type=local:AuthorizationWindow, IsDesignTimeCreatable=True}"
        mc:Ignorable="d"
        Title="Авторизация" Height="500" Width="400">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <local:BooleanToVisibilityConverter x:Key="BooleanToVisibility"/>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="0.4*"/>
        </Grid.RowDefinitions>

        <Frame x:Name="MainFrame"
               NavigationUIVisibility="Hidden"
               HorizontalContentAlignment="Stretch"
               VerticalContentAlignment="Stretch"
               HorizontalAlignment="Stretch"
               VerticalAlignment="Stretch" />

        <Button Grid.Row="1"
                Content="Зарегистрироваться"
                Style="{StaticResource SmallButton}"
                Click="Register_OnClick" 
                VerticalAlignment="Bottom"
                HorizontalAlignment="Center"
                x:Name="MainButton"/>
                
        <Button Grid.Row="1"
                Content="Войти как гость"
                Style="{StaticResource SmallButton}"
                Click="AuthorizationGuest_OnClick"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Right"
                Visibility="{Binding CanAuthGuest, Converter={StaticResource BooleanToVisibility}, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</Window>
